import { Typography } from '@material-ui/core';

function SignUp() {
    return (
        <div>
            <Typography variant="h1">Sign Up Body</Typography>
        </div>
    );
}

export default SignUp;
